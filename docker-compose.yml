version: "3.8"

services:
  vendor:
    build:
      context: .
      dockerfile: docker/Dockerfile.vendor
    container_name: vendor
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: vortex
    depends_on:
      - database
    ports:
      - 80:80
    volumes:
      - ./:/var/www/html/:cached
  database:
    image: mysql:8.0
    restart: always
    container_name: database
    environment:
      MYSQL_ROOT_PASSWORD: ${DATABASE_ROOT_PASSWORD} #password that will be set for the root superuser account
      MYSQL_DATABASE: ${DATABASE_NAME} #app_db
      # MYSQL_USER:  #optional
      # MYSQL_PASSWORD:  #optional
    ports:
      - 3306:3306
    volumes:
      - db_data:/var/lib/mysql
  phpmyadmin:
    image: phpmyadmin
    restart: always
    links:
      - database
    ports:
      - 8080:80
    environment:
      PMA_HOST: database
      PMA_PORT: 3306
      PMA_ARBITRARY: 1

volumes:
  db_data:
