{% extends 'base.html.twig' %}

{% block title %}FeedArticle{% endblock %}

{% block body %}
    <h1>FeedArticle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ feed_article.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ feed_article.title }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ feed_article.link }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ feed_article.content }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ feed_article.date }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_feed_article_index') }}">back to list</a>

    <a href="{{ path('app_feed_article_edit', {'id': feed_article.id}) }}">edit</a>

    {{ include('feed_article/_delete_form.html.twig') }}
{% endblock %}
